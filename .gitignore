node_modules/
dist/
out/
*.log

# Keep build/ for icon assets — only ignore any temp files there
build/*.iconset/
build/*.tmp

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
*.pem
*.key

# Build artifacts
*.dmg
*.zip
*.pkg
*.exe
*.deb
*.rpm
*.AppImage

# Swift build outputs
NookSwift/build/
NookSwift/Nook_bin
NookSwift/test_runner
NookSwift/Nook.xcodeproj/xcuserdata/
NookSwift/Nook.xcodeproj/project.xcworkspace/xcuserdata/

# IDE
.vscode/
.idea/
*.swp
*.swo
*.swn

# User data — IMPORTANT: never commit real tasks/passwords
data/
*.tasks.json
build/user-data-backup-*.tar.gz
*backup*.tar.gz

# Dev notes (not public)
record.md


# Logs
logs/
npm-debug.log*
yarn-debug.log*

# Website source recordings — raw .mov files for Screen Studio editing
# (final web-friendly mp4 lives at website/assets/images/, that one IS tracked)
recordings/

# seed-demo backups (real task snapshots while running demos)
**/_demo_backups/

# verify-ui screenshot output (本地视觉校验产物)
verification/
